; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[platformio]
default_envs = esp32dev

[common]
build_flags =
	-DLWJSON_IGNORE_USER_OPTS
app_src_filter =
    +<app>
    -<factory>
    +<*.c*>
factory_src_filter =
    +<factory>
    -<factory/page>
    -<app>
    +<*.c*>

[env]
platform = espressif32
framework = arduino
extra_scripts = pre:scripts/pio_db_include_toolchain.py
board_build.partitions = partitions.csv
lib_deps =
	fastled/FastLED@3.10.3
	https://github.com/MaJerle/lwjson.git#2a8c724d039cd6d6bd9f7b91d3d710bce9acdb6a
	knolleary/PubSubClient@^2.8
	mishamyrt/LightComposer@2.0.2

[env:esp32dev]
board = esp32dev
build_flags =
    ${common.build_flags}
	-DCONFIG_LIGHT_PIN_CTL=25

[env:esp32dev-app]
extends = env:esp32dev
build_src_filter = ${common.app_src_filter}

[env:esp32dev-factory]
extends = env:esp32dev
build_src_filter = ${common.factory_src_filter}

[env:s2-mini]
board = lolin_s2_mini
build_flags =
    ${common.build_flags}
	-DCONFIG_LIGHT_PIN_CTL=18

[env:s2-mini-app]
extends = env:s2-mini
build_src_filter = ${common.app_src_filter}

[env:s2-mini-factory]
extends = env:s2-mini
build_src_filter = ${common.factory_src_filter}
